# 部署到 Vercel 時，在 Dashboard → Settings → Environment Variables 設定以下兩個變數。
# 值指向你的 Render 後端網址（VITE_* 於 build 時寫死，改值後需重新 Deploy 才生效）。

# 後端 REST API 位址（https）
VITE_API_URL=https://travel-backend-xxxx.onrender.com

# 後端 WebSocket 位址（注意是 wss，不是 ws）
VITE_WS_URL=wss://travel-backend-xxxx.onrender.com
